import React from 'react';
import Toggle from './modetoggleButton.js';

const DarkModeToggle = () => {
	// 
	
	// const darkMode = useDarkMode(initialState, darkModeConfig);

	return (
		);
	};
	
	export default DarkModeToggle;